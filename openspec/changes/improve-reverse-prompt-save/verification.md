## 验证结果
- `node tests/reverse-prompt-save.test.mjs` 通过：建议名称、自定义名称、分组入参、位置记忆、重复拦截、失败重试、加载恢复、保存前失效库与分组检查、真实反推页分析按钮集成。
- `node tests/prompt-reverse.test.mjs` 通过：导航及已有页面转交功能。
- `openspec validate improve-reverse-prompt-save --strict` 通过。
- localhost:3000 浏览器实测：常驻保存区、按“风格”分组真实保存、刷新记住位置、重复按钮禁用、名称建议、原自定义名称保留。已观察布局无横向溢出。
- 验证副本 `tpl_d7740863d7ed` 已通过接口精确清理，原库两条提示词 ID 完整保留；恢复原草稿名称并关闭测试标签。

## 范围
修改反推页 HTML/CSS/JavaScript，新增独立保存控制脚本和相关测试。未改动后端、模型调用和其他页面。
查重为提交前前端读取校验，不承诺跨标签并发写入的原子幂等。

## 单屏布局追加验证
- 展开主侧栏，分别验证 1920×1080、1366×768、1024×768：反推工作区高度分别为 1046、734、734 px；开始反推、用于生图、用于编辑、保存按钮均完整位于工作区内。
- 输入与输出面板的 scrollHeight 等于 clientHeight，面板无需整体滚动；长提示词在正文框内滚动。笔记本与较窄窗口正文框仍分别保留 302、256 px 高度。
- 本次仅调整反推页 HTML 保存字段布局与 CSS；没有新增模型请求、改写提示词或保存数据。极小移动窗口保留既有响应式布局。
