# 验证记录

- 13 个前端 `.test.mjs` 文件全部通过；新增工作台覆盖变量替换、生成/编辑路由、重复点击防护、草稿恢复、配方保存/应用/删除、恢复任务只查询不重发、来源快照与画布连线。
- Python 本地状态存储 4 项测试通过：原子保存、多记录并发、版本冲突/删除版本保留、坏文件不覆盖和输入限制。
- main.py、存储模块 Python 编译检查通过；工作台与画布脚本语法检查通过；OpenSpec 严格校验通过。
- 浏览器真实调用 AI Gateway 的 ChatGPT 网页版/gpt-image-2：生成红围巾马克笔旅行者，刷新后接回原任务；一句话编辑为蓝围巾。人工查看两张本地图片，角色、地图册、姿态和画风保持，围巾颜色按要求变化。
- 原图 `/assets/output/online_8e1f8f185e.png`，修改图 `/assets/output/online_01094396e5.png`，均为 1024×1536。
- 浏览器验证配方保存、应用、更新；首张结果自动成为模板效果图；结果与请求快照刷新保留。
- 独立页面及主页 iframe 两条路径均验证：送新画布、来源详情、创建分支、返回工作台带原结果作为参考图。修复主页仅 hash 变化未重新加载分支的问题。
- 1280×900 导航固定展开：三栏同排约 187/246/526 px，右栏完整；1440×1000 验证画布操作。恢复测试视口并关闭测试标签。
- 仅重启本站 3000 服务，未改动 3300 网关。保留真实生成示例与常用配方供使用。

## 边界
- 未给所有内置模板批量生成效果图；无封面的模板显示暂无效果图，可上传、使用结果设定或首次成功生成后自动保存。
- 浏览器刷新能接回任务；后端服务重启后内存任务会过期，明确提示核对历史，不自动重发。
- 旧画布图片没有记录的来源信息不会补造。新增画布来源快照未对所有原有生成引擎逐一实测。
