## ADDED Requirements
### Requirement: 完整提示词正文
系统 SHALL 提供单一正文输入和默认收起的可选备注，完整保存用户输入。
#### Scenario: 旧条目编辑
- **WHEN** 打开含正向和负向的旧条目
- **THEN** 两部分完整显示于正文，保存再打开不重复负向段落。
#### Scenario: 画布应用
- **WHEN** 用户复用提示词
- **THEN** 正文包含旧负向约束且不包含备注，完整应用仍保留参数建议。
