## 1. Contract tests

- [x] 1.1 Add failing static contracts for the text-to-image custom provider eligibility, iframe cache versioning, and CLI address label.

## 2. Provider availability fixes

- [x] 2.1 Extend local text-to-image provider filtering to include eligible custom image APIs.
- [x] 2.2 Update local-function iframe and API-settings script resource versions.
- [x] 2.3 Show local CLI login state instead of an unconfigured address for CLI platforms.

## 3. Verification

- [x] 3.1 Run relevant static tests and strict OpenSpec validation; review all scenarios without submitting an external generation request.
