## 1. 判断契约与接口

- [x] 1.1 实现审核、分类配置和结构化结果校验模块，覆盖固定出口、稳定 ID、类别顺序、长度限制及非法返回场景。
- [x] 1.2 新增 `/api/canvas-vision-judge`，复用现有 API 服务商和图片转换路径，保持普通 LLM 返回契约不变；覆盖图片失效、不支持视觉、超时及异常响应。
- [x] 1.3 实现固定 JSON 提示词和简短判断依据输出，明确区分 `unknown`、`fail` 与执行失败，不自动修复、重试或更换模型。

## 2. 分支执行基础

- [x] 2.1 实现可独立验证的分支图计划与校验，纳入输出中转依赖，拒绝回路、跨出口汇合、多个循环控制器及悬空出口。
- [x] 2.2 为含视觉判断的流程接入运行、轮次、节点隔离的上下文与输出映射，实现公共上游去重、命中出口激活及未命中后继跳过。
- [x] 2.3 适配图片与文本输入读取，按出口传原图，按连线配置附加本轮依据或建议，禁止读取历史输出。
- [x] 2.4 接入固定次数串行与并行循环，覆盖并行乱序结果、每轮单图限制及一出口多下游。
- [x] 2.5 实现停止、失败后停止派发、迟到响应隔离、刷新中断和手动重试，验证不重复已成功上游、兄弟分支或轮次。

## 3. 画布界面

- [x] 3.1 增加视觉判断创建入口、节点布局、审核与分类配置、稳定出口和连线文本选项，覆盖删类别及切换模式的影响提示。
- [x] 3.2 增加测试判断、运行此流程、停止、结果区和轮次查看，配置变更后标注历史结果，区分无法判断与执行失败。
- [x] 3.3 统一含判断节点的流程运行入口，支持未接后续出口正常结束；补齐文字状态、键盘连接和可访问状态提示。

## 4. 保存与兼容

- [x] 4.1 保存加载、复制、撤销重做及连线清理保留新字段，旧节点与旧连线保持原义，错误图不静默删线。
- [x] 4.2 实现普通版本 1 和新能力版本 2 的导入导出，校验必需能力并提示旧版限制，验证新工作流往返和不导出凭证。

## 5. 验收

- [x] 5.1 用可控模型响应完成审核三个出口、分类顺序、非法结果、串联判断、分支汇合拒绝、重试与停止等关键自动化验证。
- [x] 5.2 回归不含新节点的普通 LLM、生成节点、输出中转、原有串行与并行循环及旧画布加载。
- [x] 5.3 在浏览器验收节点配置、出口连线、命中与跳过状态、结果轮次及键盘操作，保存必要截图证据。
- [x] 5.4 使用画布当前默认的视觉服务商完成单图审核和分类联调（用户未另行指定，已告知默认选择），记录模型、输入和分支证据；如缺少条件，明确保留未完成，不能用模拟测试代替真实验证。
