# 功能测试问题修复记录

日期：2026-09-22。范围：此前测试记录中已确认的问题，不包含未经测试的全部产品能力。

## 已修复

| 问题 | 处理 | 验证 |
| --- | --- | --- |
| 在线增强误报上传失败 | 选图只读取本地文件；真正运行本地增强时才上传 ComfyUI。替换参考图时清除旧上传缓存 | 浏览器上传后直接显示“开始增强”；真实在线请求成功，无需本地服务 |
| 增强完成按钮变英文、在线显示无效超分开关 | 使用中文翻译；超分开关仅在本地模型显示 | 实际页面确认 |
| 网页模型显示 4K 等精确输出承诺 | 生图、编辑、增强、角度、智能画布按网页适配器区分能力；显示原生分辨率，保留画幅选择；服务端追加画幅要求，标准 API 提示词不变 | 实际增强选择 2:3，返回 1024×1536；assets/output/online_4cf9706ec5.png |
| GPT 旧 ModelScope 设置阻塞 | 启动加载配置后校验聊天与图片模型，恢复为当前已配置列表中的有效选择并保存 | VM 回归覆盖双提供方、双模型失效场景 |
| 智能画布残留 RunningHub、火山入口 | 移除新建选项；历史节点仍保留媒体及参数，显示服务已移除并阻止执行；MiniMax 的旧 RunningHub 分支同样阻止执行 | 浏览器引擎仅显示 API 与 ComfyUI；历史 ModelScope 保留测试仍通过 |
| 资产库显示尚未接入的认证 | 只展示已支持的平台；无支持平台时隐藏注册入口，已有认证记录仍保留 | 浏览器中 AI Gateway 不再展示无效注册面板 |
| WebSocket 缺依赖 | requirements.txt 增加 websockets>=12,<16，当前内置 Python 已安装 15.0.1 | 最终进程真实握手及 ping/pong 通过 |
| 本地离线后仍尝试排队执行 | 后端检查全部不可达时立即报出 ComfyUI 未连接和处理方法，不占用后端负载 | 真实 /api/generate 返回明确离线提示；单测确认负载未增加 |

## 工作流复测

原先浏览器未捕获下载事件不足以认定导出有缺陷，本轮补齐验证：

- 从测试画布导出 ZIP：200，3709642 字节，包含 2 节点和 2 份本地图片资源，逐项检查文件长度。
- 同一 ZIP 导入接口：200；页面实际选择 ZIP 后提示“已导入 2 个节点”，输入图及裁剪图均展示。
- 样例 ZIP：C:/Users/mafuyu/AppData/Local/Temp/qa-fixes-roundtrip.zip。
- 仅在“功能验证 0921”测试画布追加节点，未操作原用户画布。

## 自动检查

- node --test tests/*.test.mjs：11 个测试文件通过。
- 新增 Python 画幅测试：4/4 通过；本地离线测试：1/1 通过。
- 图片接口生成/编辑回归：11/11 通过。
- JavaScript 语法、Python 编译、git diff --check、OpenSpec strict 校验通过。
- 全量 Python 测试并非全绿：原有 test_canvas_log_cleanup.py 的 14 项测试引用当前已不存在的日志清理函数。与本轮修改前的源码备份对比，确认该接口此前已缺失；没有为通过旧测试恢复涉及文件删除的旧实现，也没有删除测试。此项作为独立测试维护问题保留。

## 运行与边界

- 网站 3000 已重启，最终 PID 6664；日志目录 C:/Users/mafuyu/AppData/Local/Temp/studio-qa-fixes-final-20260922-002421。
- 网关 3300 本轮未重新构建，已修复的界面和原图过滤保持。
- ComfyUI 127.0.0.1:8188 仍未运行。项目未提供其安装目录/启动程序，本轮修复的是离线处理，不是本地模型运行环境；未安装整套 ComfyUI 或下载模型。
- 网页模型画幅通过提示词表达，仍可能受上游能力限制，不能保证任意画幅和精确像素。
- 修改前文件备份在 C:/Users/mafuyu/AppData/Local/Temp/studio-qa-fix-backup-20260922-001148。
- 未提交、推送；既有未提交改动保持。

## 后续修复：无需安装 ComfyUI（2026-09-22）

以下结果更新上方首次验证时的遗留项和运行状态：

- ComfyUI 是可选本地执行环境，默认关闭。四个图像工具默认选择已配置在线模型；不存在在线模型时明确提示配置。历史本地画布节点保留，关闭状态阻止本地执行，不自动换用付费或其他在线提供方。
- 智能画布和经典画布隐藏未启用的本地新建入口；设置收为“本地模型（可选）”。浏览器验证启用后显示本地选项、关闭后移除，验证结束保持关闭。
- 过期 test_canvas_log_cleanup.py 已替换为 test_canvas_persistence.py，覆盖保存、删日志不删图片、过期版本冲突、同毫秒/时钟回拨版本保护、回收站恢复。没有恢复已移除的删图接口。修改前测试保存在本轮备份。
- 修复真实保存缺陷：同一毫秒连续写入或系统时间回拨时，updated_at 仍单调递增，旧页面不能凭相同版本覆盖新数据。
- 全量 Python 回归 29 项通过，前端 11 个测试文件通过。JavaScript 语法和 OpenSpec strict 校验通过。
- 网站 3000 最终重启为 PID 10708，日志 C:/Users/mafuyu/AppData/Local/Temp/studio-optional-local-final-20260922-004428。已实际确认可选设置标题、默认关闭状态和智能画布在线模型界面。
- 本轮未安装 ComfyUI、未执行本地模型生图；在线能力无需此环境。网关 3300 本轮未修改。
- 本轮修改前备份：C:/Users/mafuyu/AppData/Local/Temp/studio-online-default-20260922-003333。
