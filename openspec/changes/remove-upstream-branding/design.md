## Context

上游合并仍处于未提交状态，本次在其基础上修改，保留已有合并成果。用户已确认停用上游自动更新。

## Goals / Non-Goals

目标：移除个人宣传，保留 README 来源说明，阻止旧更新入口覆盖代码。
不涉及模型调用、用户数据、许可证变更或提交推送。

## Decisions

删除作者组件和对应样式、API 设置推广内容；保留可用服务商配置但删除返佣参数。项目主页固定指向 fork。后端更新、检测、连通性和回滚接口返回 410，旧页面也无法覆盖代码；前端移除更新按钮并停止启动时检测。

## Risks / Trade-offs

旧页面缓存仍可调用旧接口 → 后端停用并重启当前服务验证。前端旧更新弹窗及请求逻辑已删除；后端接口替换为明确拒绝，未被路由调用的底层下载辅助函数暂保留，不扩大为整段重构。
