## 1. 清理

- [x] 1.1 移除作者组件、推广入口和返佣参数，更新 README 来源说明
- [x] 1.2 停用更新入口及后端接口，项目主页指向 fork

## 2. 验证

- [x] 2.1 检查语法、既有回归测试及接口拒绝行为
- [x] 2.2 重启服务并确认页面和来源说明

## 验证结果

- 6 项现有 Node 回归测试通过，修改的 Python/JavaScript 语法检查通过，git diff --check 通过。
- 隔离测试中 5 个旧更新相关接口均返回 410，屏蔽网络请求后测试仍通过；app-info 返回本 fork 地址和 updates_enabled=false。
- 已重启本地 3000 服务（PID 77856），实际 /api/check-update 返回 410。
- 浏览器刷新后确认侧栏不含作者、社交链接或更新入口，API 设置不含 DX-OS 横幅。
- README 保留原作者与原仓库致谢；LICENSE 无修改。服务启动会按项目原逻辑刷新 HTML 资源版本号。
- 未提交、推送。上次合并保持待提交状态。
