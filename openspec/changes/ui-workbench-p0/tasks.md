## 1. 基础规范（P0）

- [x] 1.1 取消整页自动缩放，改为响应式布局；保留画布自身缩放
- [x] 1.2 在 theme.css 定义共享字号、间距、颜色、圆角、控件高度令牌
- [x] 1.3 共享图片控件 cli-image-tools.js 接入令牌（字号与控件高度）
- [x] 1.4 导航宽度改为显式按钮控制并持久化，hover 只提示名称
- [x] 1.5 修复 API 设置推荐卡片裁切与中英文混用
- [x] 1.6 修复词条缓存：提升 i18n 模块版本号并刷新各页面资源版本号

## 2. 文生图样板页（P1）

- [x] 2.1 改为 340px 参数栏 + 弹性结果区布局
- [x] 2.2 主按钮固定在参数栏底部，窄容器下紧随参数区可达
- [x] 2.3 结果区提供"结果 / 历史"切换、contain 展示与最近结果缩略图
- [x] 2.4 保留三个引擎分支与请求字段，主按钮不再以协议命名
- [x] 2.5 补充缺失的中文词条，移除无意义英文大写标签
- [x] 2.6 引擎切换可读性：对比度 4.34:1 → 6.95:1、选中态加描边环、图标统一 14px 并消除段内溢出
- [x] 2.7 共享尺寸控件补「宽度 / 高度」可见标签（原先只有 aria-label）
- [x] 2.8 修正参数栏比例：参数区不再撑高，主按钮紧跟控件（原先按钮被约 350px 空白推到底部）；提示词固定 160px；最近结果为空时不占位

## 3. 验证

- [x] 3.1 运行 node --test tests/*.test.mjs
- [x] 3.2 语法检查改动的 JS
- [x] 3.3 在 1440x900 与 743x731 下截图并检查横向溢出
- [x] 3.4 拦截 fetch 核实本地与 CLI 两条生成链路的请求字段
- [x] 3.5 API 设置页在 1440x900、1196x900 与 743x731 下的卡片边界验证
- [x] 3.6 全站 12 个页面扫描裸 i18n key，均为 0

## 4. 后续（未开始）

- [ ] 4.1 按同一骨架迁移在线生图、图片编辑、细节增强、角度控制
- [ ] 4.2 素材库详情抽屉、画布列表网格、对话页空态
