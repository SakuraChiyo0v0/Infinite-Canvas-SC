## Context

当前 HEAD 为 9c379e4，与上游 67f49e4 的共同基点为 06eb6f5。双方修改 main.py 和多个页面，存在合并冲突风险。

## Goals / Non-Goals

目标：同步上游并保留现有图像工具功能和运行数据。
不包含：Docker 改动、提交、推送、部署或外部模型调用。

## Decisions

- 采用 Git 三方合并并停在提交前，逐处审查冲突；不整体覆盖文件。
- 合并前创建备份分支，并在 Git 私有目录备份本地配置及记录运行数据哈希。
- 上游安装和画布逻辑优先使用新版，定制远程引擎、输出尺寸、服务就绪状态保持。
- 使用现有 Node 回归测试、上游 Python 清理测试及语法检查验证。

## Risks / Trade-offs

- 自动合并可能存在语义冲突 → 对比双方涉及的函数并执行回归测试。
- 真实生图需要账号和计费调用 → 本次只做离线验证，明确说明边界。

## Migration Plan

无需数据迁移。保留备份分支及配置副本，合并未提交；必要时可中止合并恢复。
