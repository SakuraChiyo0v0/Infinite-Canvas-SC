## ADDED Requirements

### Requirement: 同步上游并保留定制图片工具
系统 SHALL 包含上游 67f49e4 的更新，同时保持现有远程图片引擎、服务就绪状态、分类历史和输出尺寸能力。

#### Scenario: 同步后使用图片工具
- **WHEN** 用户在文生图、细节增强、编辑或角度控制中选择已配置远程服务
- **THEN** 工具仍按所选服务和尺寸提交请求并保留对应历史类别

### Requirement: 保留运行数据
同步过程 MUST 保持现有本地配置与运行数据内容不变。

#### Scenario: 完成合并
- **WHEN** 上游代码完成合并
- **THEN** 本地 API/.env、运行配置、历史及素材文件与合并前一致，并可定位原分支备份
