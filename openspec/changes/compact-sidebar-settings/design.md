## Context
侧栏有固定展开和图标折叠两种状态，主题/语言已有切换逻辑。
## Goals / Non-Goals
减少视觉层级冲突，保持操作与键盘可达；不调整功能页。
## Decisions
复用现有折叠组和按钮事件；设置组使用紧凑列表，折叠时 inert 防止焦点进入。使用新的折叠存储版本让首次默认收起，其后保留选择。主页与版本使用页脚容器，折叠态竖排。
## Risks / Trade-offs
暗色及窄栏样式覆盖 → 浏览器检查展开、收起和主题切换。
